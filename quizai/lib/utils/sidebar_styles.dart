import "package:flutter/material.dart";

class SideBarStyles {
    static const int closedNavbarWidth = 60;
    static const int openedNavbarWidth = 600;
    static bool isNavbarOpen = true;
    static Duration animationDuration = const Duration(milliseconds: 500);
}
